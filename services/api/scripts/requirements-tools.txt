# Dependencias de scripts/ (operación Dynamo + Lambda).
# Ver services/api/README.md → «Herramientas Python».
#   cd services/api && python3 -m venv .venv && source .venv/bin/activate && pip install -r scripts/requirements-tools.txt
boto3>=1.34
